// PostCSS configuration for Muraasala landing page.
// Because this project is using ECMAScript modules ("type": "module" in
// package.json), we export the configuration as an ES module default
// instead of using CommonJS `module.exports`.

export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {}
  }
};